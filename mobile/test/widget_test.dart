import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test básico — la app arranca sin crash
    // Tests de integración completos se agregan en Sprint 2
    expect(true, isTrue);
  });
}
